import { Settings } from '@flama/design-system-web/icons';

/** The organization's own pane in Settings: its key in the URL, and its icon. */
export const GENERAL_SETTINGS_SECTION = { key: 'general', icon: Settings } as const;
